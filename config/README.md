# config

## Description

This directory contains the main configuration files required to run **bal_pipe**.

## Contents

- `config.yaml` – Main pipeline configuration, including sample definitions, computational resources, software parameters, database locations and output directories.

## Notes

Before running the workflow, users should modify `config.yaml` to match their own dataset and analysis requirements.